server:
  port: 3000

spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://127.0.0.1:3306/comsystem?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  #JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    # formateSQL
    properties:
      hibernate:
        format_sql: true
  mvc:
    favicon:
      enabled: false

  # email
  mail:
    default-encoding: UTF-8
    host: smtp.qq.com
    username: 370578047@qq.com
    password: rkcvspccxaykbhfc
    properties:
      mail:
        smtp:
          ssl:
            enable: true
